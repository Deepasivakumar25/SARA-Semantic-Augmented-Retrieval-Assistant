# Install project dependencies in Google Colab with:
# !pip install -q transformers sentencepiece accelerate pypdf sentence-transformers faiss-cpu

# For a local environment, install from requirements.txt instead.
